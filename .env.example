# Used by the root docker-compose.yml to pass secrets into the backend
# container. Copy this to .env in the project root before running
# `docker compose up`.

JWT_SECRET=
GROQ_API_KEY=
GROQ_MODEL=openai/gpt-oss-120b
VOYAGE_API_KEY=
NEXT_PUBLIC_API_URL=http://localhost:4000
